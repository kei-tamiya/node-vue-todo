/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports) {

	// Generated by CoffeeScript 1.11.1
	(function() {
	  var socket, tvm;

	  socket = io();

	  Vue.component('task-item', {
	    template: '<div><h2 v-bind:class="{completed: task.completed}"><span class="taskCompleteCheck"><input type="checkbox" v-on:click="$emit(\'toggle-complete\')" v-model="task.completed" /></span>{{ task.title }}<span class="pull-right"><input class="btn" type="button" v-on:click="$emit(\'delete-task\')" value="x" /></span></div>',
	    props: ['task']
	  });

	  tvm = new Vue({
	    el: "#wrapTodos",
	    mounted: function() {
	      return this.$http.get("/api/rooms/" + ($('#roomId').text()) + "/tasks/get").then(function(response) {
	        return this.tasks = response.body.data;
	      }, function(response) {
	        return console.log(response);
	      });
	    },
	    data: {
	      newTask: "",
	      tasks: []
	    },
	    methods: {
	      addTask: function() {
	        var nt, t;
	        nt = this.newTask.trim();
	        if (!nt) {
	          return;
	        }
	        t = {
	          room_id: $('#roomId').text(),
	          title: nt,
	          completed: false
	        };
	        this.$http.post("/api/rooms/" + ($('#roomId').text()) + "/tasks/post", t).then(function(response) {
	          return socket.emit('addTask', response.body);
	        }, function(response) {
	          return console.log(response);
	        });
	        return this.newTask = "";
	      },
	      toggleComplete: function(index) {
	        var b, t;
	        b = !this.tasks[index].completed;
	        t = Object.assign({}, this.tasks[index], {
	          completed: b
	        });
	        return this.$http.patch("/api/rooms/" + ($('#roomId').text()) + "/tasks/update", {
	          body: t
	        }).then(function() {
	          return socket.emit('toggleComplete', {
	            index: index,
	            completed: b
	          });
	        }, function(response) {
	          return console.log(response);
	        });
	      },
	      updateTitle: function(index) {
	        var t;
	        t = Object.assign({}, this.tasks[index], {});
	        return this.$http.patch("/api/rooms/" + ($('#roomId').text()) + "/tasks/update", {
	          body: t
	        }).then(function() {
	          return socket.emit('updateTitle', {
	            index: index,
	            title: t.title
	          });
	        }, function(response) {
	          return console.log(response);
	        });
	      },
	      deleteTask: function(index) {
	        var t;
	        t = Object.assign({}, this.tasks[index]);
	        if (confirm("「" + t.title + "」　を削除してよろしいですか？")) {
	          return this.$http["delete"]("/api/rooms/" + ($('#roomId').text()) + "/tasks/delete", {
	            body: t
	          }).then(function() {
	            return socket.emit('deleteTask', index);
	          }, function(response) {
	            return console.log(response);
	          });
	        }
	      }
	    }
	  });

	  socket.on('addTask', function(task) {
	    return tvm.tasks.push(task);
	  });

	  socket.on('toggleComplete', function(data) {
	    return tvm.tasks[data.index].completed = data.completed;
	  });

	  socket.on('updateTitle', function(data) {
	    return tvm.tasks[data.index].title = data.title;
	  });

	  socket.on('deleteTask', function(index) {
	    return tvm.tasks.splice(index, 1);
	  });

	}).call(this);

	//# sourceMappingURL=task.js.map


/***/ }
/******/ ]);