doctype html
html(lang="ja")
    head
        meta(charset="utf-8")
        style(src="css/style.css")
        title Node-Vue-Todo | todos
    body
        h1 Node-Vue-Todo | Todos#index
        if todos
            each todo in todos
                div #{todo.title}